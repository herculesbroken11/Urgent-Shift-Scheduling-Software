
Investigating the issue: virtual appointments (modality = `opi` or `vri`) show a blank Location column in appointment lists because they don't have a physical `location_id`.

Let me check the relevant files to confirm where this displays.
